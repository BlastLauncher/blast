{
  "name": "blast-workspaces",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm run test",
    "dev": "pnpm run dev",
    "build-renderer": "pnpm --filter blast-renderer run build ",
    "start-renderer:dev": "pnpm --filter blast-renderer run start:dev ",
    "start-renderer": "pnpm --filter blast-renderer run start ",
    "fmt": "prettier --write .",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "Yukai Huang",
  "license": "MIT",
  "jest": {
    "testEnvironment": "node",
    "verbose": true
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=6"
  }
}
